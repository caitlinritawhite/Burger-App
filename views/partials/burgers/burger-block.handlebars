<li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-newDevour="{{devour}}">
		{{#if devoured}}DEVOUR{{else}}RE-ORDER!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
